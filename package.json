{
  "name": "church-platform",
  "version": "0.1.0",
  "private": true,
  "workspaces": ["frontend", "backend"],
  "scripts": {
    "dev": "frontend/node_modules/.bin/vite --config frontend/vite.config.ts --host --port 8080 frontend",
    "build": "cd frontend && ./node_modules/.bin/vite build",
    "preview": "cd frontend && ./node_modules/.bin/vite preview --host --port 8080"
  }
}
