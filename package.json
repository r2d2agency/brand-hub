{
  "name": "church-platform",
  "version": "0.1.0",
  "private": true,
  "workspaces": ["frontend", "backend"],
  "scripts": {
    "dev": "echo 'Use: cd frontend && npm run dev — em outro terminal: cd backend && npm run dev'",
    "build": "cd frontend && npm run build",
    "preview": "cd frontend && npm run preview"
  }
}
