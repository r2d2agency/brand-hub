{
  "name": "church-platform",
  "version": "0.1.0",
  "private": true,
  "workspaces": ["frontend", "backend"],
  "scripts": {
    "dev": "bun --cwd frontend run dev -- --host --port 8080",
    "build": "bun --cwd frontend run build",
    "preview": "bun --cwd frontend run preview"
  }
}
